@model IEnumerable<SmithSwimmingSchool_NickMorales.Models.Report>

@{
    ViewData["Title"] = "Reportes de Progreso";
}

<h1 class="text-center mb-4">Reportes de Progreso</h1>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-warning text-center">
        No hay reportes disponibles para este grupo.
        <br />
        <a href="@Url.Action("CoursesBySwimmer")" class="btn btn-primary mt-3">Back</a>
    </div>
}
else
{
    <div class="container">
        <div class="row justify-content-center">
            @foreach (var report in Model)
            {
                <div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-header bg-primary text-white">
                            <h5 class="card-title mb-0">Reporte @report.ReportID</h5>
                        </div>
                        <div class="card-body">
                            <p class="card-text"><strong>Descripción: </strong>@report.Description</p>
                        </div>
                    </div>
                </div>
            }
            <br />
            <div>
                <a asp-action="CoursesBySwimmer" class="btn btn-secondary">Back to Courses</a>
            </div>
        </div>
    </div>
}
